'use strict';

// constants definition

// Size
const TINY_SIZE: string = 'tiny';
const SMALL_SIZE: string = 'small';
const MEDIUM_SIZE: string = 'medium';
const LARGE_SIZE: string = 'large';
const EXTRA_LARGE_SIZE: string = 'extraLarge';
const CUSTOM_SIZE: string = 'custom';

// Font family
const THIN_FONT: string = 'thin';
const LIGHT_FONT: string = 'light';
const REGULAR_FONT: string = 'regular';
const BOLD_FONT: string = 'bold';

// Dimensions
const EXCESS_VERTICAL_SPACING_PERCENTAGE: number = 20;

// exports
export {
    TINY_SIZE,
    SMALL_SIZE,
    MEDIUM_SIZE,
    LARGE_SIZE,
    EXTRA_LARGE_SIZE,
    CUSTOM_SIZE,

    THIN_FONT,
    LIGHT_FONT,
    REGULAR_FONT,
    BOLD_FONT,

    EXCESS_VERTICAL_SPACING_PERCENTAGE,
}
