'use strict';

// external imports
import {mergeDeepRight} from 'ramda';

// local imports
import baseTheme from './../base_theme';

// theme implementation
const theme = mergeDeepRight(baseTheme, {

});

// exports
export default Object.freeze(theme);