'use strict';

// @flow

// external imports

// local imports
import * as baseTheme from './../base_theme';

import {colorPaletteFunc, baseStylesFunc} from './style_partials';
import {extendTheme} from './../../helpers/theme_helpers';

// exports
export const blueGreyRed = extendTheme(
    baseTheme.fontFacesFunc,
    baseTheme.fontStacksFunc,
    colorPaletteFunc,
    baseTheme.materialDepthLevelsFunc,
    baseStylesFunc
);
